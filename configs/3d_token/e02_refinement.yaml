extends: base.yaml

model:
  decoder:
    split:
      enabled: true
      dense: true
    refinement:
      num_layers: 4

training:
  checkpoint_dir: ./experiments/checkpoints/3d_token_e02
  wandb_exp_name: 3D_Token_MVP_E02_Refinement
