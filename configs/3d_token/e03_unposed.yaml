extends: e02_refinement.yaml

# Replace this class and checkpoint after the posed decoder is validated.
model:
  backbone:
    class_name: model.backbones.mvp.frozen.FrozenMVPBackbone
    checkpoint_path: ./mvp.pt

training:
  checkpoint_dir: ./experiments/checkpoints/3d_token_e03
  wandb_exp_name: 3D_Token_MVP_E03_Unposed
