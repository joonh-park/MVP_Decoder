extends: e02_refinement.yaml

# Replace this class and config after the posed decoder is validated.
model:
  backbone:
    class_name: model.backbones.mvp.backbone.MVPBackbone
    config_path: configs/mvp/inference.yaml

training:
  checkpoint_dir: ./experiments/checkpoints/3d_token_e03
  wandb_exp_name: 3D_Token_MVP_E03_Unposed
