extends: base.yaml

model:
  decoder:
    split:
      enabled: true
      dense: true
    refinement:
      num_layers: 0

training:
  checkpoint_dir: ./experiments/checkpoints/3d_token_e01
  wandb_exp_name: 3D_Token_MVP_E01_Latent_Split
